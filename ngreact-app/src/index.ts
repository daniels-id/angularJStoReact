import angular from 'angular';
import './app/app.module'; // Import the main application module

// The ng-app="app" in index.html automatically bootstraps the 'app' module.
// No manual bootstrapping is needed here unless you have specific requirements. 